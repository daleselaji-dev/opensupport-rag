# Legacy folder notice

The numbered roadmap now uses `v0_5_contextual_hierarchical` for Contextual /
Parent-Child RAG and `v0_9_production_rag` for production operations. This
folder remains as a historical placeholder so old links do not point at the
wrong stage.
